import 'package:flutter/material.dart';

class prakash extends StatelessWidget {
  const prakash({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("prakash")),
      body: Container(color: Colors.amber),
    );
  }
}
